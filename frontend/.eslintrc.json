{
  // If we use a next.js project, if not, check other config requirements
  "extends": ["next/core-web-vitals", "prettier", "next"],
  "rules": {
    "no-console": ["error", { "allow": ["warn", "error", "info"] }],
    "no-debugger": "error"
  }
}
